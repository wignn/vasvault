# GET /api/v1/workspaces/:id

Method: GET

URL: /api/v1/workspaces/:id

Auth: Bearer (required)

Path params:

- `id` (workspace id)

Response (200):

```json
{ "id":1, "name":"Team Vault", "description":"Shared storage", "members":[] }
```
