# GET /api/v1/categories/:id

Method: GET

URL: /api/v1/categories/:id

Auth: Bearer (required)

Path params:

- `id` (category id)

Response (200):

```json
{ "id":1, "name":"Invoices", "color":"#FF0000" }
```
